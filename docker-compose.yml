services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: busymap
      POSTGRES_USER: busymap
      POSTGRES_PASSWORD: busymap
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
