services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: packedplaces
      POSTGRES_USER: packedplaces
      POSTGRES_PASSWORD: packedplaces
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
